{
  "name": "@veilforge/contracts",
  "version": "1.8.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "hardhat test",
    "deploy:arc": "hardhat run scripts/deploy.ts --network arcTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17",
    "typescript": "^5.7.2"
  }
}
