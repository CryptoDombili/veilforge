import test from'node:test';import assert from'node:assert/strict';import{calculateMetrics}from'../../../packages/benchmark/src/index.js';
const result={caseId:'X',domain:'arc-payments',kind:'negative',passed:true,compileMatch:true,matches:[],falsePositives:[],falseNegatives:[],missingIncompleteReasons:[],unsafeLocations:[],actualFindings:[]};
test('metrics separate finding/case/domain/kind and zero denominators',()=>{const metrics=calculateMetrics([result]);assert.equal(metrics.overall.findingLevel.precision,'not-applicable');assert.equal(metrics.overall.caseLevel.trueNegatives,1);assert.ok(metrics.perDomain['arc-payments']);assert.ok(metrics.kinds.negative);});
test('per-detector finding totals partition overall findings without cross-detector leakage',()=>{const findings=[{...result,passed:false,matches:[{detectorId:'arc-payments.event-disclosure',issues:[]}],falsePositives:[{detectorId:'arc-payments.calldata-observation',fingerprint:'fp'}],actualFindings:[{detectorId:'arc-payments.event-disclosure',fingerprint:'tp'},{detectorId:'arc-payments.calldata-observation',fingerprint:'fp'}]}];const metrics=calculateMetrics(findings),sum=field=>Object.values(metrics.perDetector).reduce((n,x)=>n+x.findingLevel[field],0);assert.equal(sum('truePositives'),metrics.overall.findingLevel.truePositives);assert.equal(sum('falsePositives'),metrics.overall.findingLevel.falsePositives);assert.equal(sum('falseNegatives'),metrics.overall.findingLevel.falseNegatives);});
