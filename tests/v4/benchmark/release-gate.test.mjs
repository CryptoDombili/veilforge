import test from'node:test';import assert from'node:assert/strict';import{evaluateReleaseGate}from'../../../packages/benchmark/src/index.js';
const clean=Array.from({length:60},(_,i)=>({caseId:String(i),kind:'negative',compileMatch:true,falseNegatives:[],falsePositives:[],missingIncompleteReasons:[],unsafeLocations:[],actualFindings:[],reportHash:null,reportIntegrityVerified:false,sarifIntegrityVerified:false}));
test('release gate passes complete clean corpus',()=>assert.equal(evaluateReleaseGate({results:clean,nondeterministicResults:0}).status,'passed'));
test('release gate fails false negatives unsafe paths and coverage',()=>{const altered=structuredClone(clean.slice(1));altered[0].falseNegatives=[{detectorId:'x'}];altered[0].unsafeLocations=['x'];const decision=evaluateReleaseGate({results:altered});assert.equal(decision.status,'failed');assert.ok(decision.reasons.includes('corpus-coverage'));assert.ok(decision.reasons.includes('falseNegatives'));assert.ok(decision.reasons.includes('unsafeLocations'));});
