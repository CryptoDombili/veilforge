import test from 'node:test'; import assert from 'node:assert/strict'; import { detect, results } from './helpers.mjs';
test('sensitive treasury value to unknown external call',()=>{const {detectorRun}=detect('contract TreasuryCase {function f(address target,bytes32 settlementReference) external {target.call(abi.encode(settlementReference));}}'); assert.ok(results(detectorRun,'external-call-disclosure').length);});
test('resolved internal call negative',()=>{const {detectorRun}=detect('contract TreasuryCase {function g(address) internal{} function f(address beneficiary) external{g(beneficiary);}}'); assert.equal(results(detectorRun,'external-call-disclosure').length,0);});
test('resolved library call negative',()=>{const {detectorRun}=detect('library L{function g(address)internal pure{}} contract TreasuryCase{function f(address beneficiary)external pure{L.g(beneficiary);}}'); assert.equal(results(detectorRun,'external-call-disclosure').length,0);});
test('approval reference to external call',()=>{const {detectorRun}=detect('contract TreasuryCase {function f(address target,bytes32 approvalReference) external {target.call(abi.encode(approvalReference));}}'); assert.ok(results(detectorRun,'approval-disclosure').length);});
