import test from'node:test';import assert from'node:assert/strict';import{detect,policy,results}from'./helpers.mjs';
const source='contract CreditCase{event Out(bytes32);function approvedCommitment(bytes32 x)internal pure returns(bytes32){return x;}function publish(bytes32 settlementReference)external{emit Out(approvedCommitment(settlementReference));}}';
test('policy-approved wrapper',()=>{const p={...policy,approvedWrappers:[{id:'W1',callable:'CreditCase.approvedCommitment(bytes32)',kind:'commitment',scope:'CreditCase.publish(bytes32)'}]};const{detectorRun}=detect(source,p);assert.ok(results(detectorRun,'event-disclosure').some(x=>x.disposition==='policy-approved'));});
test('policy public field',()=>{const p={...policy,publicFields:[{id:'P1',field:'customer-kyc-reference',justification:'public',scope:'CreditCase.f(bytes32)'}]};const{detectorRun}=detect('contract CreditCase{event Out(bytes32);function f(bytes32 kycReference)external{emit Out(kycReference);}}',p);assert.ok(results(detectorRun,'event-disclosure').some(x=>x.disposition==='policy-approved'));});
test('plain keccak remains detected',()=>{const{detectorRun}=detect('contract CreditCase{event Out(bytes32);function f(bytes32 kycReference)external{emit Out(keccak256(abi.encode(kycReference)));}}');assert.ok(results(detectorRun,'event-disclosure').every(x=>x.disposition!=='policy-approved'));});
test('misleading private encrypt wrapper remains detected',()=>{const{detectorRun}=detect(source.replaceAll('approvedCommitment','privateEncrypt'));assert.ok(results(detectorRun,'event-disclosure').every(x=>x.disposition!=='policy-approved'));});
