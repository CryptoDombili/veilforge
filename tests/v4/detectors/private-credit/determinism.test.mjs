import test from'node:test';import assert from'node:assert/strict';import{serializeDetectorRun}from'../../../../packages/analyzer/src/v4/detectors/index.js';import{detect,detectSources,policy}from'./helpers.mjs';
const contract='// SPDX-License-Identifier: MIT\npragma solidity 0.8.24;\ncontract CreditCase{event Out(address);function f(address borrower)external{emit Out(borrower);emit Out(borrower);}}\n';
test('deterministic insertion order',()=>{const s=contract.split('\n').slice(2).join('\n'),a=detect(s,{...policy,publicFields:[],approvedWrappers:[]}).detectorRun,b=detect(s,{...policy,approvedWrappers:[],publicFields:[]}).detectorRun;assert.equal(serializeDetectorRun(a),serializeDetectorRun(b));});
test('Windows path LF CRLF and BOM are irrelevant',()=>{const a=detectSources({'contracts/Credit.sol':contract}),b=detectSources({'contracts\\Credit.sol':`\uFEFF${contract.replace(/\n/gu,'\r\n')}`});assert.equal(serializeDetectorRun(a),serializeDetectorRun(b));});
test('same input has same IDs fingerprints and serialization',()=>{const s=contract.split('\n').slice(2).join('\n'),a=detect(s).detectorRun,b=detect(s).detectorRun;assert.deepEqual(a.results.map(x=>[x.detectorResultId,x.fingerprint]),b.results.map(x=>[x.detectorResultId,x.fingerprint]));assert.equal(serializeDetectorRun(a),serializeDetectorRun(b));});
