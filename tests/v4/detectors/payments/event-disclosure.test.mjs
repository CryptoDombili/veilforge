import test from 'node:test'; import assert from 'node:assert/strict'; import { detect, results } from './helpers.mjs';
test('payer to event', () => { const { detectorRun } = detect('contract PaymentCase { event Out(address); function f(address payer) external { emit Out(payer); } }'); assert.ok(results(detectorRun, 'event-disclosure').some((item) => item.dataClass === 'payer')); });
test('payee to event', () => { const { detectorRun } = detect('contract PaymentCase { event Out(address); function f(address payee) external { emit Out(payee); } }'); assert.ok(results(detectorRun, 'event-disclosure').some((item) => item.dataClass === 'payee')); });
test('beneficiary to indexed event argument', () => { const { detectorRun } = detect('contract PaymentCase { event Out(address indexed who); function f(address beneficiary) external { emit Out(beneficiary); } }'); assert.ok(results(detectorRun, 'event-disclosure').some((item) => item.dataClass === 'beneficiary')); });
test('payment amount to event', () => { const { detectorRun } = detect('contract PaymentCase { event Out(uint); function f(uint amount) external { emit Out(amount); } }'); assert.ok(results(detectorRun, 'event-disclosure').some((item) => item.dataClass === 'amount')); });
test('invoice reference to event', () => { const { detectorRun } = detect('contract PaymentCase { event Out(bytes32); function f(bytes32 invoiceReference) external { emit Out(invoiceReference); } }'); assert.ok(results(detectorRun, 'event-disclosure').some((item) => item.dataClass === 'invoice-reference')); });
