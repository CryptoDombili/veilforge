import test from'node:test';import assert from'node:assert/strict';import{report}from'../report/helpers.mjs';import{canonicalReportBytes,calculateReportHash,reportHashPayload,sha256Digest,validateReport,verifyReportIntegrity}from'../../../packages/analyzer/src/v4/report/index.js';
function legacy(){const value=structuredClone(report());value.schemaVersion='4.0.0';value.reportVersion='4.0.0';value.scanner.reportSchemaVersion='4.0.0';value.scanner.findingSchemaVersion='1.0.0';for(const f of value.findings)for(const key of['detectorId','detectorVersion','relatedDetectorIds','stableRuleKey','titleKey'])delete f[key];value.integrity.hashPayloadVersion='veilforge.report.hash.v1';value.integrity.findingsDigest=sha256Digest(value.findings);value.integrity.reportHash=null;value.integrity.canonicalByteLength=canonicalReportBytes(reportHashPayload(value)).length;value.integrity.reportHash=calculateReportHash(value);return value;}
test('legacy 4.0.0 hash v1 remains explicitly verifiable',()=>{const value=legacy();assert.equal(validateReport(value),true);assert.equal(verifyReportIntegrity(value),true);});
test('new report uses 4.1.0 and hash v2 and rejects mismatched version pairs',()=>{const value=report();assert.equal(value.reportVersion,'4.1.0');assert.equal(value.integrity.hashPayloadVersion,'veilforge.report.hash.v2');const bad=structuredClone(value);bad.integrity.hashPayloadVersion='veilforge.report.hash.v1';assert.equal(verifyReportIntegrity(bad),false);});
