import test from 'node:test'; import assert from 'node:assert/strict'; import { EXIT_CODES } from '../../../packages/cli/src/index.js';
test('exit code contract is fixed and unique', () => { assert.deepEqual(EXIT_CODES, { COMPLETED: 0, GENERIC: 1, ARGUMENT: 2, SOURCE: 3, SCAN_FAILED: 4, INCOMPLETE: 5, TIMEOUT: 6, ABORTED: 7, REPORT_INVALID: 8, EXPORT_INVALID: 9, OUTPUT: 10, PROTOCOL: 11 }); assert.equal(new Set(Object.values(EXIT_CODES)).size, 12); });
