import test from'node:test';import assert from'node:assert/strict';import{finding,projectFindingRun,serializePresentation}from'./helpers.mjs';
test('same Finding input yields the same projection',()=>{const f=finding({token:'same'});assert.equal(serializePresentation(projectFindingRun({findingRunId:'run',findings:[f]})),serializePresentation(projectFindingRun({findingRunId:'run',findings:[f]})));});
test('Windows paths canonicalize in presentation views',()=>{const a=finding({token:'path',sinkLocation:{sourcePath:'src/A.sol',byteStart:1,byteEnd:2}}),b=finding({token:'path',sinkLocation:{sourcePath:'src\\A.sol',byteStart:1,byteEnd:2}});assert.equal(serializePresentation(projectFindingRun({findingRunId:'run',findings:[a]})),serializePresentation(projectFindingRun({findingRunId:'run',findings:[b]})));});
test('LF CRLF and BOM variants do not affect presentation',()=>{const a=finding({token:'bom',sinkLocation:{sourcePath:'src/A.sol',byteStart:1,byteEnd:2}}),b=finding({token:'bom',sinkLocation:{sourcePath:'\uFEFFsrc/A.sol',byteStart:1,byteEnd:2}});assert.equal(serializePresentation(projectFindingRun({findingRunId:'run',findings:[a]})),serializePresentation(projectFindingRun({findingRunId:'run',findings:[b]})));});
test('finding insertion order does not change canonical projection',()=>{const a=finding({token:'i1'}),b=finding({token:'i2',domain:'arc-treasury',detectorId:'arc-treasury.event-disclosure'});assert.equal(serializePresentation(projectFindingRun({findingRunId:'run',findings:[a,b]})),serializePresentation(projectFindingRun({findingRunId:'run',findings:[b,a]})));});
