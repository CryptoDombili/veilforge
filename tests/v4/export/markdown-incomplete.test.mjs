import test from'node:test';import assert from'node:assert/strict';import{renderMarkdownReport}from'../../../packages/analyzer/src/v4/export/index.js';import{report}from'./helpers.mjs';
test('incomplete analysis cannot look like a clean no-findings result',()=>{const md=renderMarkdownReport(report({analysis:{statuses:{ir:'incomplete'},incompleteReasons:['unsupported-expression']},findingRun:{summary:{total:0}},presentationRun:{findings:[]}}));assert.match(md,/Analysis.*incomplete/);assert.match(md,/unsupported-expression/);assert.match(md,/Manual review is required/);});
test('finding incomplete messages and boundaries are visible',()=>{const value=report();value.findings[0].disposition='incomplete';value.findings[0].incompleteMessages=[{reason:'unresolved-call',message:'Manual call review required.'}];value.analysis.unresolvedBoundaries={call:'external'};const md=renderMarkdownReport(value);assert.match(md,/Manual call review required/);assert.match(md,/call.*external/);});
