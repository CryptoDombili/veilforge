import test from'node:test';import assert from'node:assert/strict';import{escapeMarkdown,renderMarkdownReport}from'../../../packages/analyzer/src/v4/export/index.js';import{report}from'./helpers.mjs';
test('pipe backtick heading and link syntax cannot inject Markdown',()=>{const value=escapeMarkdown('| `x` # [click](javascript:alert(1))');assert.equal(value.includes('\\|'),true);assert.equal(value.includes('\\`'),true);assert.equal(value.includes('\\#'),true);assert.equal(value.includes('\\['),true);});
test('raw HTML is escaped and control characters are normalized',()=>{const value=escapeMarkdown('<script>x</script>\u0001\r\nnext');assert.doesNotMatch(value,/<script>/u);assert.match(value,/&lt;script&gt;/u);assert.doesNotMatch(value,/\u0001|\r|\n/u);});
test('extensions and sensitive literals are not rendered',()=>{const value=report();value.extensions.secret='-----BEGIN PRIVATE KEY-----';value.scan.operational={hostname:'private-host'};const md=renderMarkdownReport(value);assert.doesNotMatch(md,/PRIVATE KEY|private-host/u);});
