import test from'node:test';import assert from'node:assert/strict';import{renderMarkdownReport}from'../../../packages/analyzer/src/v4/export/index.js';import{report}from'./helpers.mjs';
test('evidence table carries role type context confidence and completeness without source code',()=>{const value=report(),f=value.findings[0];f.evidenceView=[{role:'source',evidenceType:'source-classification',location:{sourcePath:'contracts/Loan.sol',line:42,column:17},contractIds:['contract-1'],callableIds:['callable-1'],confidenceContribution:'high',completenessMarker:'complete'}];const md=renderMarkdownReport(value);for(const token of['source','source-classification','contracts/Loan.sol:42:17','contract-1','callable-1','high','complete'])assert.ok(md.includes(token));assert.doesNotMatch(md,/contract\s+Loan\s*\{/u);});
test('trace is ordered and reports omitted count while preserving source and sink',()=>{const value=report(),f=value.findings[0];f.traceView={steps:[{stepType:'source-location',valueRole:'source',location:f.sourceLocations[0]},{stepType:'sink-location',valueRole:'sink',location:f.sinkLocations[0]}],omittedStepCount:8};const md=renderMarkdownReport(value);assert.ok(md.indexOf('source-location')<md.indexOf('sink-location'));assert.match(md,/Omitted steps: 8/);});
test('absolute locations are never emitted',()=>{const value=report();value.findings[0].primaryLocation={sourcePath:'C:\\Users\\person\\secret.sol',line:1};const md=renderMarkdownReport(value);assert.doesNotMatch(md,/C:\\Users|person|secret\.sol/u);});
