import test from'node:test';import assert from'node:assert/strict';import{createScanSession,runScanStage,runRemainingStages,SCAN_STAGES,serializeScanSession}from'../../../packages/analyzer/src/v4/orchestration/index.js';import{corpusInput,scanCase}from'./helpers.mjs';
test('staged API reaches the same deterministic result as single call',async()=>{const single=await scanCase('PAY-POS-001'),session=createScanSession(corpusInput('PAY-POS-001'));for(const stage of SCAN_STAGES.slice(0,5))await runScanStage(session,stage);await runRemainingStages(session);assert.equal(serializeScanSession(session),serializeScanSession(single.session));});
test('stage order is enforced',async()=>{const session=createScanSession(corpusInput('PAY-POS-001'));await assert.rejects(()=>runScanStage(session,'compilation'),{code:'SCAN_STAGE_ORDER_INVALID'});});
