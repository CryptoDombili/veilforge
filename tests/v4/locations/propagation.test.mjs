import test from'node:test';import assert from'node:assert/strict';import{scanCase}from'../orchestration/helpers.mjs';import{renderSarif}from'../../../packages/sarif/src/index.js';
test('finding evidence trace report and SARIF preserve real regions',async()=>{const result=await scanCase('PAY-POS-002'),finding=result.report.findings[0];for(const loc of[finding.primaryLocation,...finding.sourceLocations,...finding.sinkLocations]){assert.match(loc.sourcePath,/^src\//);assert.ok(loc.startByte>=0&&loc.endByte>=loc.startByte);assert.ok(loc.startLine>0&&loc.startColumn>0&&loc.endLine>0&&loc.endColumn>0);}assert.ok(finding.evidenceView.some(x=>x.location?.startLine));assert.ok(finding.traceView.steps.some(x=>x.location?.startLine));const physical=renderSarif(result.report).runs[0].results[0].locations[0].physicalLocation;assert.ok(physical.region.startLine>0&&physical.region.startColumn>0);assert.ok(physical.properties.startByte>=0&&physical.properties.endByte>=physical.properties.startByte);assert.equal(JSON.stringify(result.report).includes(process.cwd()),false);});
